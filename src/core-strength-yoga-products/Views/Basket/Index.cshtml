@model (IEnumerable<core_strength_yoga_products.Models.BasketItem>, decimal)

<table class="table">
    @*<thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model.Item1 => Model.Item1)
            </th>
            <th>
                @Html.DisplayNameFor(Model.Item1.)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>*@
    <tbody>
        @foreach (var item in Model.Item1)
        {
            <tr>
                <td>
                    <a href="/Shop/Product?ProductId=@item.ProductId">@item.Product.Name</a>
                </td>
                <td>
                    <a href="/Shop/Product?ProductId=@item.ProductId"><img src="@item.Product.Image.Path" class="basket-img"/></a>
                </td>
                <td>
                    Quantity: @item.Quantity
                </td>
                <td>
                    Item Total: €@item.TotalCost.ToString("0.00")
                </td>
                <td>
                    Colour: @item.Colour
                </td>
                <td>
                    Size: @item.Size
                </td>
               <td>
                    @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |*@
                    <a asp-action="DeleteFromBasket" asp-route-productId="@item.ProductId"
                   asp-route-productAttributeId="@item.ProductAttributeId" class="primary-btn checkout-btn">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<h5>Order Total: €@Model.Item2.ToString("0.00")</h5>
<a href="/Order/Index"<input type="submit" class="primary-btn checkout-btn"/>Place Order</a>



